pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
              sh 'mkdir dir7'
            }
        }
        stage('Test') { 
            steps {
              sh 'mkdir dir8'
            }
        }
        stage('Deploy') { 
            steps {
               sh 'mkdir dir9'
            }
        }
    }
}
